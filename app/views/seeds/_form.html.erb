<%= form_with(model: seed, local: true) do |form| %>
  <% if seed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seed.errors.count, "error") %> prohibited this seed from being saved:</h2>

      <ul>
      <% seed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"シード名" %>
    <%= form.text_field :title, id: :seed_title %>
  </div>

  <div class="field">
    <%= form.label :"発案者" %>
    <%= form.text_field :owner, id: :seed_owner %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
